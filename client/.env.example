# Copy to .env for local overrides, or set in your host's dashboard for production.

# Base URL of the API. Leave blank in dev — Vite proxies /api to localhost:4000.
# In production set it to your deployed API, e.g. https://agriopen-api.onrender.com
VITE_API_URL=
